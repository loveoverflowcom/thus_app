name: thus_cli
description: Thus terminal CLI client for the chat platform.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.4.2
  fpdart: ^1.1.0
  thus_auth:
    path: ../../packages/thus_auth
  thus_contacts:
    path: ../../packages/thus_contacts
  thus_core:
    path: ../../packages/thus_core
  thus_messaging:
    path: ../../packages/thus_messaging
  thus_network:
    path: ../../packages/thus_network
  thus_storage:
    path: ../../packages/thus_storage

dev_dependencies:
  lints: ^6.0.0
  very_good_analysis: ^6.0.0
