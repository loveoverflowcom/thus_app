name: thus_storage
description: Local storage helpers for Hive CE with secure boxes and caching.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  hive_ce: ^2.5.0
  thus_core:
    path: ../thus_core

dev_dependencies:
  lints: ^6.0.0
  very_good_analysis: ^6.0.0
