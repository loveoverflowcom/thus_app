name: thus_core
description: Core primitives (failure, usecase, logging, DI) shared across Thus apps.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  equatable: ^2.0.5
  meta: ^1.11.0

dev_dependencies:
  lints: ^6.0.0
  very_good_analysis: ^6.0.0
