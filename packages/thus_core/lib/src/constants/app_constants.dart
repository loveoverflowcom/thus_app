class AppConstants {
  const AppConstants._();

  static const defaultApiBaseUrl = 'https://api.thus.local';
  static const websocketPath = '/ws/chat';
  static const hiveEncryptionBox = 'secure_box';
}
