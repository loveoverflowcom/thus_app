name: thus_contacts
description: Contacts domain (profiles, contact requests, contacts) for Thus apps.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.3
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.8
  flutter_bloc: ^8.1.6
  fpdart: ^1.1.0
  thus_auth:
    path: ../thus_auth
  thus_core:
    path: ../thus_core
  thus_network:
    path: ../thus_network

dev_dependencies:
  lints: ^6.0.0
  very_good_analysis: ^6.0.0
